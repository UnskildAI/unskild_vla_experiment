experiment_name: vlm_finetune
output_dir: ./outputs/vlm_finetune
seed: 42
model:
  name: paligemma
  extra:
    vision_frozen: false
    language_frozen: false
data:
  dataset_name: vlm
  batch_size: 8
  extra:
    max_length: 512
    image_size: 224
training:
  max_steps: 1000
  learning_rate: 1.0e-5
  log_interval: 10
  checkpoint_interval: 500
