experiment_name: action_policy
output_dir: ./outputs/action_policy
seed: 42
model:
  name: vision_action
  extra:
    input_dim: 2048
    action_dim: 7
data:
  dataset_name: action
  batch_size: 32
  extra:
    action_dim: 7
training:
  max_steps: 10000
  learning_rate: 1.0e-4
  log_interval: 10
  checkpoint_interval: 1000
